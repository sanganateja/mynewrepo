pipeline {
 agent any
 stages {
        stage('Build') {
            steps {
                echo 'Building'
			}	
        }
		stage('Build Docker Image'){
	        steps {
			    sh 'docker build -t sangana/new-nginx .'
	        }
		}
        stage ('test'){
	        steps {
		        echo 'testing'
			}
	    }
       stage('Push Docker Image'){
	       
